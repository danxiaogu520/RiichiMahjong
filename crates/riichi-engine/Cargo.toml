[package]
name = "riichi-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
riichi-core = { path = "../riichi-core" }
riichi-logic = { path = "../riichi-logic" }
rand = "0.8"
thiserror = "2"
serde = { version = "1", features = ["derive" ]}
